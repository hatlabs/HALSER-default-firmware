; HALSER Default Firmware
; NMEA 0183 to NMEA 2000 gateway with SensESP

[platformio]
; espidf builds ESP-IDF from source, so sdkconfig.defaults is authoritative.
; That enables CONFIG_MBEDTLS_DYNAMIC_BUFFER, which a TLS Signal K connection
; on the ESP32-C3 needs.
default_envs = halser_espidf

[env:halser_espidf]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
board = esp32-c3-devkitm-1
framework = espidf, arduino
lib_ldf_mode = deep

upload_speed = 2000000
monitor_speed = 115200

lib_deps =
    SignalK/SensESP @ ^3.2.0
    SensESP/NMEA0183 @ ^3.2.0
    adafruit/Adafruit NeoPixel @ ^1.12
    fastled/FastLED @ 3.9.4
    ttlappalainen/NMEA2000-library @ ^4.17.2
    NMEA2000_twai=https://github.com/skarlsson/NMEA2000_twai

build_flags =
    -D SENSESP_BUTTON_PIN=9
    -D PIN_RGB_LED=8
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -D SENSESP_SSL_SUPPORT=1
    -D CORE_DEBUG_LEVEL=ARDUHAL_LOG_LEVEL_VERBOSE
    -D USE_ESP_IDF_LOG

build_unflags = -Werror=reorder
board_build.partitions = min_spiffs.csv
; esp_insights and esp_rainmaker are pulled in transitively by arduino-esp32 on
; esp32c3 and require their server certs embedded, even though this firmware
; uses neither cloud service.
board_build.embed_txtfiles =
    managed_components/espressif__esp_insights/server_certs/https_server.crt
    managed_components/espressif__esp_rainmaker/server_certs/rmaker_mqtt_server.crt
    managed_components/espressif__esp_rainmaker/server_certs/rmaker_claim_service_server.crt
    managed_components/espressif__esp_rainmaker/server_certs/rmaker_ota_server.crt
monitor_filters = esp32_exception_decoder
